import express from 'express';
const router = express.Router();

// Placeholder booking routes - implement as needed
router.get('/', (req, res) => {
  res.json({ message: 'Booking routes - implement as needed' });
});

export default router;
