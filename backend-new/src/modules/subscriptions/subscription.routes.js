import express from 'express';
const router = express.Router();

// Placeholder subscription routes - implement as needed
router.get('/', (req, res) => {
  res.json({ message: 'Subscription routes - implement as needed' });
});

export default router;
