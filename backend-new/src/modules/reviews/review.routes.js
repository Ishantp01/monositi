import express from 'express';
const router = express.Router();

// Placeholder review routes - implement as needed
router.get('/', (req, res) => {
  res.json({ message: 'Review routes - implement as needed' });
});

export default router;
